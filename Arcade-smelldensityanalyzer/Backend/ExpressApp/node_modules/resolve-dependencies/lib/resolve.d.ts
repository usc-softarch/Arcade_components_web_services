import { File } from './file';
import { ResolveDepOptions } from './options';
import { resolveSync } from './node-loader';
export default function resolve(...options: (Partial<ResolveDepOptions> | string)[]): Promise<{
    files: import("./file").FileMap;
    entries: {
        [key: string]: File;
    };
    warnings: string[];
}>;
export { resolve, resolveSync };
